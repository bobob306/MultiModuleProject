<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <receiver
            android:name=".presentation.widget.BabyCareWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/baby_care_widget_info" />
        </receiver>

        <service
            android:name=".presentation.feeding.FeedingTimerService"
            android:foregroundServiceType="specialUse"
            android:exported="false"
            tools:targetApi="34">
            <property
                android:name="android.app.special_use_explanation"
                android:value="The feeding timer needs to run in the background to ensure accurate tracking while the user performs other tasks or leaves the app." />
        </service>
    </application>
</manifest>
